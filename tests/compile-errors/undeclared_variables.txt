# A variable declared twice, a duplicated parameter, and uses of names that
# were never declared.

program undeclaredVariables;
var x, x : int;

int f (a : int, a : int) [
    { return a; }
];

main {
    y = 5;
    x = a;
    print("result = ", w);
}
end
