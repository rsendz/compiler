# Functions cannot reach the global variables: a function sees only its own
# parameters and locals, so 'total' is undeclared inside 'accumulate'.

program scopeIsolation;
var total : int;

int accumulate ( n : int ) [
   {  return n + total; }
];

main {
    total = 1;
    print(accumulate(2));
}
end
