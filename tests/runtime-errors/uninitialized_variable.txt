# 'b' is declared but never assigned. Reading it is a runtime error: cells are
# reserved but not initialized.

program uninitializedVariable;
var a, b : int;

main {
    a = b + 1;
    print(a);
}
end
